# Phase 69 - Diagnostics report import GUI wiring polish

Polishes the diagnostics comparison report import action wiring so the hotkeys/info tab presents the action as a read-only support/debug review tool.

## Safety contract

- Review only.
- Executable payload import remains blocked.
- Helper launch remains blocked.
- Install execution remains blocked.
- Non-dry-run update behavior remains blocked.
- The GUI button and tooltip must clearly communicate the read-only safety boundary.
