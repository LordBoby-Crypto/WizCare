# Diagnostics report import GUI polish

Use the GUI `Import diagnostics report` action to open an exported diagnostics comparison report bundle. The dialog summarizes whether the bundle is safe, blocked, malformed, or incomplete and shows the report headline, severity, differences, errors, and next steps.

This workflow is support/debug-only and must not be used as permission to install an update.
